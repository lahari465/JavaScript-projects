Faulty Calculator

A JavaScript-based calculator that intentionally produces an incorrect result 10% of the time. It uses Math.random() to decide whether to perform the correct operation or replace it with a predefined incorrect operation using an operator-mapping object.
